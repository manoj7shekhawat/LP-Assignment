# LP-Assignment - bootstrap

1. First enable the Cloud Storage API by running: `gcloud services enable storage.googleapis.com`
2. create buckets using below commands:
   a. `terraform init`
   b. `terraform plan`
   c. `terraform apply`